# Byte-identical copy of firmware/claude_buddy/net_bridge_core.h — the
# platform-free SenseCraft wire-format core (see the diff-guard note in the
# header). scripts/check_shared_files.sh enforces the sync.
idf_component_register(INCLUDE_DIRS ".")
