# ArduinoJson v7.4.3 single-header release, vendored (matches the Arduino
# library version the Wio build uses). Header-only, no Arduino dependency in
# its core deserializer — data.h uses it identically on both targets.
idf_component_register(INCLUDE_DIRS "include")
